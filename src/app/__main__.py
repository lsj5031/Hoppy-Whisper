"""Command-line entry point for the Parakeet tray application placeholder."""

from __future__ import annotations


def main() -> None:
    """Stub entry point that keeps PyInstaller packaging green in CI."""
    message = (
        "Parakeet placeholder binary. "
        "Full functionality arrives in later milestones."
    )
    print(message)


if __name__ == "__main__":
    main()
